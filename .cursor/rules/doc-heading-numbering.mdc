---
description: 技术文档标题层级编号（篇名加粗 + 1.1. / 1.1.1.）
globs: docs/**/*.md
alwaysApply: false
---

# 技术文档标题编号规范（3_技术文档）

编辑 `docs/` 下 Markdown 时，**篇名不使用一级标题 `#`**；**正文最高从二级标题 `##` 起**，编号为 **1.1.、1.1.1.** 形式（逻辑上的「1.」体现在篇名加粗行，不占用 `#`）。

## 篇名（非标题）

- 文首第一行（代码块外）：`**1. 篇名**`（加粗，可独占一行后空行）。
- **禁止**再使用 `# 1. 篇名` 作为 ATX 一级标题（避免站点目录里出现多余 H1、与章节层级混淆）。

## 层级格式

| Markdown | 编号格式 | 示例 |
|----------|----------|------|
| 篇名（非标题） | `**1. …**` | `**1. MCP 协议**` |
| H2 `##` | `{1}.{2}.` | `## 1.1. MCP 是什么` |
| H3 `###` | `{1}.{2}.{3}.` | `### 1.2.1. Tools（工具调用）` |
| H4 `####` | `{1}.{2}.{3}.{4}.` | `#### 1.2.1.1. 子款标题` |
| H5–H6 | 继续向下缀一层序号 | 类推 |

## 计数规则

- **全篇不出现一级 `#` 标题**：脚本在编号完成后会把第一个 `# …` 转为加粗篇名；附录中多余的 `#` 会先降为 `##` 再编号。
- **子级归零**：出现新的 H2 时，其下 H3、H4 从 1 递增；出现新 H3 时，其下 H4 从 1 递增。
- **缺级补齐**：若文首直接从 `##` 开始（无篇名行），脚本视为隐含逻辑章节 `1`，仍输出 `## 1.1.` 起。
- **句点**：编号末级后必须有 **句点**，再接空格与标题，例如 `## 1.1. 标题`。

## 附录与长文粘贴区

- 附录入口用 **`## 1.n.`**；附录内后续标题按 **`##` / `###` 深度**继续编号。

## 代码块

- 围栏代码块内的 `#` / `##` **不参与**篇名与章节编号。

## 文首元信息

- 「上一节 / 下一节 / 导读」等用表格或引用块，**不要用 `#` 标题**。

## 批量整理

- 使用 `scripts/renumber_headings.py`：`demote_extra_h1` → `process_markdown` → `first_h1_to_bold_doc_title`。

## `docs/README.md`

- 总览页篇名同样用 **`**1. CW · Tech Docs**`**，不用 `#`。
