---
description: 技术文档标题无数字编号、小节用居中 H1
globs: docs/**/*.md
alwaysApply: false
---

# 技术文档标题规范（3_技术文档）

## 篇名

- 文首使用**居中加粗**（非 ATX 标题），**不要**篇名前序号：  
  `<p align="center"><strong>大模型基础与架构</strong></p>`  
- 不要使用 `# 篇名`，也不要 `**1. 篇名**` 这类带数字前缀。

## 正文章节（原「1.1.」级）

- **每个大节**使用 **HTML 一级标题并居中**，**标题文字不带** `1.1.` 等编号：  
  `<h1 align="center">为什么需要大模型</h1>`
- 对应过去 Markdown 里的 `## 1.1. 为什么需要大模型` —— 已统一改为上式。

## 子节

- 子节用 **Markdown ATX**，**不带数字前缀**，且比原层级**少一个 `#`**（因大节已用 `<h1>`）：
  - 原 `### 1.2.1. Self-Attention` → `## Self-Attention 原理`
  - 原 `#### …` → `### …`

## 代码块

- 围栏代码块内的 `#` 注释**不要**按标题改写。

## 批量整理

- 使用 `scripts/normalize_headings.py` 对 `docs/**/*.md` 做一次转换。  
- `scripts/renumber_headings.py` 为**旧版**（数字分级 `1.1.`）逻辑，新规范下**勿再执行**，除非有意恢复旧样式。

## 渲染说明

- `<h1 align="center">` 与 `<p align="center">` 依赖 HTML；MkDocs / Docsify / GitHub 通常可渲染。若某主题吞掉 HTML，再考虑主题级 CSS。
